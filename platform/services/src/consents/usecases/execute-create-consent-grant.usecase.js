/**
 * ExecuteCreateConsentGrant - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteCreateConsentGrant {
    context;
    idGenerator;
    consent;
    constructor(context, idGenerator, consent) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.consent = consent;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.cnsId();
        // All field names extracted from OpenAPI spec: attributeNames, purpose, relyingPartyId, subjectId
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.purpose) {
            throw new ValidationError("purpose is required");
        }
        if (!input.subjectId) {
            throw new ValidationError("subjectId is required");
        }
        if (!input.relyingPartyId) {
            throw new ValidationError("relyingPartyId is required");
        }
        if (!input.attributeNames) {
            throw new ValidationError("attributeNames is required");
        } // Check policy
        if (!CanCreate.allow({}, "")) {
            throw new PermissionDeniedError("Operation not allowed");
        }
        // Generate entity ID in services layer using IdGeneratorService (consents domain)
        const entityId = this.idGenerator.cnsId();
        // Create entity via repository (pass ID and correlation ID for response meta)
        const entity = await this.consent.createConsentGrant({ ...input, id: entityId, createdByActorId: input.createdByActorId ?? this.context.getUserId(), createdByActorType: input.createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId });
        return entity;
    }
}
