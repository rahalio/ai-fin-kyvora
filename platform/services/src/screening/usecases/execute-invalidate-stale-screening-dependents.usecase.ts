/**
 * ExecuteInvalidateStaleScreeningDependents - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: screening
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteInvalidateStaleScreeningDependents Use Case
 *
 * DDD: Application use case for get InvalidateDependent.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { InvalidateStaleScreeningDependentsInput, InvalidateStaleScreeningDependentsOutput } from "../dto/invalidate-dependent.dto";
import type { ExecutionContextService, IdGeneratorService } from "@kyvora/services/_shared/index.js";
import type { InvalidateDependentRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteInvalidateStaleScreeningDependents {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly invalidateDependent: InvalidateDependentRepository
  ) {}

  async execute(input: InvalidateStaleScreeningDependentsInput): Promise<InvalidateStaleScreeningDependentsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.scrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).snapshotId) {
      throw new ValidationError("snapshotId is required");
    }
    const snapshotId = (input as any).snapshotId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.invalidateDependent.invalidateStaleScreeningDependents({ snapshotId: snapshotId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${snapshotId}`);
    }

    return entity as InvalidateStaleScreeningDependentsOutput;
  }
}
