/**
 * ExecuteOperatorLogin - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: identity
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError } from "../errors";
export class ExecuteOperatorLogin {
    context;
    idGenerator;
    login;
    constructor(context, idGenerator, login) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.login = login;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.idnId();
        // All field names extracted from OpenAPI spec: email, password
        // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
        const entity = await this.login.operatorLogin({ ...input, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found`);
        }
        return entity;
    }
}
