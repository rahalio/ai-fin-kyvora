/**
 * ExecuteUpdateSubject - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: subjects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteUpdateSubject {
    context;
    idGenerator;
    subject;
    constructor(context, idGenerator, subject) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.subject = subject;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.subId();
        // All field names extracted from OpenAPI spec
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.subjectId) {
            throw new ValidationError("subjectId is required");
        }
        const subjectId = input.subjectId;
        // Load existing entity
        const existing = await this.subject.getSubject({ subjectId: subjectId, orgId: this.context.getOrgId(), correlationId });
        if (!existing) {
            throw new NotFoundError(`Entity not found: ${subjectId}`);
        }
        // Check policy
        if (!CanUpdate.allow({}, "")) {
            throw new PermissionDeniedError("Operation not allowed");
        }
        // Update entity via repository (pass correlation ID for response meta)
        const updated = await this.subject.updateSubject({ ...input, orgId: this.context.getOrgId(), correlationId });
        return updated;
    }
}
