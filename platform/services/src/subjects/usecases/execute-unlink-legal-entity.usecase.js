/**
 * ExecuteUnlinkLegalEntity - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: subjects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteUnlinkLegalEntity {
    context;
    idGenerator;
    legalEntityLink;
    constructor(context, idGenerator, legalEntityLink) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.legalEntityLink = legalEntityLink;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.subId();
        // All field names extracted from OpenAPI spec: none
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.linkId) {
            throw new ValidationError("linkId is required");
        }
        const linkId = input.linkId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.legalEntityLink.unlinkLegalEntity({ linkId: linkId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${linkId}`);
        }
        return entity;
    }
}
