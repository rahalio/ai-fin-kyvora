/**
 * ExecuteRefuseAttestation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: attestations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteRefuseAttestation {
    context;
    idGenerator;
    refuse;
    constructor(context, idGenerator, refuse) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.refuse = refuse;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.attId();
        // All field names extracted from OpenAPI spec: refuseReason
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.attestationId) {
            throw new ValidationError("attestationId is required");
        }
        const attestationId = input.attestationId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.refuse.refuseAttestation({ attestationId: attestationId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${attestationId}`);
        }
        return entity;
    }
}
