/**
 * ExecuteRequestAttestation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: attestations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteRequestAttestation {
    context;
    idGenerator;
    attestation;
    constructor(context, idGenerator, attestation) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.attestation = attestation;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.attId();
        // All field names extracted from OpenAPI spec: attributeNames, consentId, identityProviderId, purpose, relyingPartyId, requiredAssuranceLevel, subjectId
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.consentId) {
            throw new ValidationError("consentId is required");
        }
        const consentId = input.consentId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.attestation.requestAttestation({ consentId: consentId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${consentId}`);
        }
        return entity;
    }
}
