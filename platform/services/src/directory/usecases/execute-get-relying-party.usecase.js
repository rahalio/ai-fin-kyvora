/**
 * ExecuteGetRelyingParty - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteGetRelyingParty {
    context;
    idGenerator;
    relyingParty;
    constructor(context, idGenerator, relyingParty) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.relyingParty = relyingParty;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.dirId();
        // All field names extracted from OpenAPI spec: none
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.rpId) {
            throw new ValidationError("rpId is required");
        }
        const rpId = input.rpId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.relyingParty.getRelyingParty({ rpId: rpId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${rpId}`);
        }
        return entity;
    }
}
