/**
 * ExecuteSuspendRelyingParty - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSuspendRelyingParty Use Case
 *
 * DDD: Application use case for get Suspend.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SuspendRelyingPartyInput, SuspendRelyingPartyOutput } from "../dto/suspend.dto";
import type { ExecutionContextService, IdGeneratorService } from "@kyvora/services/_shared/index.js";
import type { SuspendRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteSuspendRelyingParty {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly suspend: SuspendRepository
  ) {}

  async execute(input: SuspendRelyingPartyInput): Promise<SuspendRelyingPartyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.dirId();

    // All field names extracted from OpenAPI spec: reason, rulebookVersion
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).rpId) {
      throw new ValidationError("rpId is required");
    }
    const rpId = (input as any).rpId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.suspend.suspendRelyingParty({ rpId: rpId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${rpId}`);
    }

    return entity as SuspendRelyingPartyOutput;
  }
}
