/**
 * ExecuteSuspendIdentityProvider - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */
import { NotFoundError, ValidationError } from "../errors";
export class ExecuteSuspendIdentityProvider {
    context;
    idGenerator;
    suspend;
    constructor(context, idGenerator, suspend) {
        this.context = context;
        this.idGenerator = idGenerator;
        this.suspend = suspend;
    }
    async execute(input) {
        // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
        const correlationId = this.idGenerator.dirId();
        // All field names extracted from OpenAPI spec: reason, rulebookVersion
        // Validate input
        if (!input) {
            throw new ValidationError("Input is required");
        }
        if (!input.idpId) {
            throw new ValidationError("idpId is required");
        }
        const idpId = input.idpId;
        // Load entity via repository (pass correlation ID for response meta)
        const entity = await this.suspend.suspendIdentityProvider({ idpId: idpId, orgId: this.context.getOrgId(), correlationId });
        if (!entity) {
            throw new NotFoundError(`Entity not found: ${idpId}`);
        }
        return entity;
    }
}
