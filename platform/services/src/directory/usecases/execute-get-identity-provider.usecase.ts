/**
 * ExecuteGetIdentityProvider - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetIdentityProvider Use Case
 *
 * DDD: Application use case for get IdentityProvider.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetIdentityProviderInput, GetIdentityProviderOutput } from "../dto/identity-provider.dto";
import type { ExecutionContextService, IdGeneratorService } from "@kyvora/services/_shared/index.js";
import type { IdentityProviderRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteGetIdentityProvider {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly identityProvider: IdentityProviderRepository
  ) {}

  async execute(input: GetIdentityProviderInput): Promise<GetIdentityProviderOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.dirId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).idpId) {
      throw new ValidationError("idpId is required");
    }
    const idpId = (input as any).idpId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.identityProvider.getIdentityProvider({ idpId: idpId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${idpId}`);
    }

    return entity as GetIdentityProviderOutput;
  }
}
