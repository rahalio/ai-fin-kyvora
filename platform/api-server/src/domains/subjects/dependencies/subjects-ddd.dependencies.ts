/**
 * Subjects DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: subjects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Subjects Domain Module - Composition root
 *
 * DDD: Composition root for subjects domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AttributeRepositoryAdapter,
  CompleteRepositoryAdapter,
  LegalEntityLinkRepositoryAdapter,
  SubjectRepositoryAdapter,
} from "@kyvora/adapters/subjects";
import { getIdGeneratorService } from "@kyvora/adapters";
import type { AdapterDynamoDBClient } from "@kyvora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCompleteRefreshCycle,
  ExecuteCreateLegalEntityLink,
  ExecuteCreateRefreshCycle,
  ExecuteCreateSubject,
  ExecuteGetSubject,
  ExecuteListLegalEntityLinks,
  ExecuteListRefreshCycles,
  ExecuteListSubjectAttributes,
  ExecuteListSubjects,
  ExecuteRegisterSubjectAttribute,
  ExecuteUnlinkLegalEntity,
  ExecuteUpdateSubject,
} from "@kyvora/services/subjects/usecases";
// Ports (interfaces)
import type {
  AttributeRepository,
  CompleteRepository,
  LegalEntityLinkRepository,
  SubjectRepository,
} from "@kyvora/services/subjects/ports";

export interface SubjectsDomainModule {
  repos: {
    attributes: AttributeRepository;
    completes: CompleteRepository;
    legalEntityLinks: LegalEntityLinkRepository;
    subjects: SubjectRepository;
  };
  useCases: {
    attributes: {
      create: ExecuteRegisterSubjectAttribute;
      list: ExecuteListSubjectAttributes;
    };
    completes: {
      create: ExecuteCompleteRefreshCycle;
    };
    legalEntityLinks: {
      create: ExecuteCreateLegalEntityLink;
      get: ExecuteUnlinkLegalEntity;
      list: ExecuteListLegalEntityLinks;
    };
    subjects: {
      create: ExecuteCreateSubject;
      get: ExecuteGetSubject;
      list: ExecuteListSubjects;
      update: ExecuteUpdateSubject;
    };
  };
}

export function buildSubjectsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): SubjectsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      attributes?: AttributeRepository;
      completes?: CompleteRepository;
      legalEntityLinks?: LegalEntityLinkRepository;
      subjects?: SubjectRepository;
    } = {};

    obj.attributes = new AttributeRepositoryAdapter(dynamoClient);
    obj.completes = new CompleteRepositoryAdapter(dynamoClient);
    obj.legalEntityLinks = new LegalEntityLinkRepositoryAdapter(dynamoClient);
    obj.subjects = new SubjectRepositoryAdapter(dynamoClient);
    return {
      attributes: obj.attributes!,
      completes: obj.completes!,
      legalEntityLinks: obj.legalEntityLinks!,
      subjects: obj.subjects!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    attributes: {
      create: new ExecuteRegisterSubjectAttribute(executionContext, idGenerator, repos.attributes),
      list: new ExecuteListSubjectAttributes(executionContext, idGenerator, repos.attributes),
    },
    completes: {
      create: new ExecuteCompleteRefreshCycle(executionContext, idGenerator, repos.completes),
    },
    legalEntityLinks: {
      create: new ExecuteCreateLegalEntityLink(executionContext, idGenerator, repos.legalEntityLinks),
      get: new ExecuteUnlinkLegalEntity(executionContext, idGenerator, repos.legalEntityLinks),
      list: new ExecuteListLegalEntityLinks(executionContext, idGenerator, repos.legalEntityLinks),
    },
    subjects: {
      create: new ExecuteCreateSubject(executionContext, idGenerator, repos.subjects),
      get: new ExecuteGetSubject(executionContext, idGenerator, repos.subjects),
      list: new ExecuteListSubjects(executionContext, idGenerator, repos.subjects),
      update: new ExecuteUpdateSubject(executionContext, idGenerator, repos.subjects),
    },
  };
  return { repos, useCases: useCases as SubjectsDomainModule["useCases"] };
}
