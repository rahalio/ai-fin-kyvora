/**
 * MembershipDecision Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { MembershipDecisionEntity } from "@kyvora/core/directory/models/index.js";
import { directorySchemas } from "@kyvora/core/directory";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "dir";

export function createMembershipDecision(overrides: Partial<MembershipDecisionEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    action: overrides.action ?? "certify",
    decidedAt: overrides.decidedAt ?? new Date().toISOString(),
    decisionId: overrides.decisionId ?? `mbd_${ulid().toLowerCase()}`,
    memberId: overrides.memberId ?? faker.string.uuid(),
    memberType: overrides.memberType ?? "identityProvider",
    reason: overrides.reason ?? faker.lorem.word(),
    ...overrides,
  };

  return directorySchemas.MembershipDecision.parse(raw);
}

export function membershipDecisionBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createMembershipDecision(overrides));
}
