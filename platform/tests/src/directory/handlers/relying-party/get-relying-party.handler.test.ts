/**
 * GetRelyingParty handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getRelyingParty } from "@kyvora/api-server/domains/directory/handlers";
import type { DirectoryDDDDependencies } from "@kyvora/api-server/domains/directory";
import { createRelyingParty as createRelyingPartyEntity } from "@kyvora/factories/directory";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@kyvora/tests/shared";
import { DIRECTORY_USE_CASES } from "../__factories__/use-cases.js";

describe("GetRelyingParty", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DirectoryDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DirectoryDDDDependencies>(
      DIRECTORY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createRelyingPartyEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetRelyingParty, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { rpId: entity.id };

    await getRelyingParty(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetRelyingParty.execute).toHaveBeenCalledWith(expect.objectContaining({ rpId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { rpId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetRelyingParty, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getRelyingParty(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
