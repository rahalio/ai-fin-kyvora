/**
 * ListRelyingParties handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: directory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listRelyingParties } from "@kyvora/api-server/domains/directory/handlers";
import type { DirectoryDDDDependencies } from "@kyvora/api-server/domains/directory";
import { createRelyingParty as createRelyingPartyEntity } from "@kyvora/factories/directory";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@kyvora/tests/shared";
import { DIRECTORY_USE_CASES } from "../__factories__/use-cases.js";

describe("ListRelyingParties", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DirectoryDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DirectoryDDDDependencies>(
      DIRECTORY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createRelyingPartyEntity({ orgId }),
      createRelyingPartyEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListRelyingParties, "execute").mockResolvedValue(useCaseResult);

    await listRelyingParties(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListRelyingParties.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
