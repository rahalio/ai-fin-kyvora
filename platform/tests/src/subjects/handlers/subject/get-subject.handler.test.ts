/**
 * GetSubject handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: subjects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSubject } from "@kyvora/api-server/domains/subjects/handlers";
import type { SubjectsDDDDependencies } from "@kyvora/api-server/domains/subjects";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@kyvora/tests/shared";
import { SUBJECTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSubject", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SubjectsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SubjectsDDDDependencies>(
      SUBJECTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSubject, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { subjectId: entity.id };

    await getSubject(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSubject.execute).toHaveBeenCalledWith(expect.objectContaining({ subjectId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { subjectId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSubject, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSubject(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
