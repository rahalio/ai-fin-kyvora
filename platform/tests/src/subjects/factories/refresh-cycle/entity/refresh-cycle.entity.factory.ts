/**
 * RefreshCycle Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: subjects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import type { RefreshCycleEntity } from "@kyvora/core/subjects/models/index.js";
import { subjectsSchemas } from "@kyvora/core/subjects";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "sub";

export function createRefreshCycle(overrides: Partial<RefreshCycleEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    cycleId: overrides.cycleId ?? `rfr_${ulid().toLowerCase()}`,
    dueAt: overrides.dueAt ?? new Date().toISOString(),
    status: overrides.status ?? "scheduled",
    subjectId: overrides.subjectId ?? `sub_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return subjectsSchemas.RefreshCycle.parse(raw);
}

export function refreshCycleBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createRefreshCycle(overrides));
}
