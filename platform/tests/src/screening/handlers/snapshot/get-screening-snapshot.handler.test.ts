/**
 * GetScreeningSnapshot handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: screening
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getScreeningSnapshot } from "@kyvora/api-server/domains/screening/handlers";
import type { ScreeningDDDDependencies } from "@kyvora/api-server/domains/screening";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@kyvora/tests/shared";
import { SCREENING_USE_CASES } from "../__factories__/use-cases.js";

describe("GetScreeningSnapshot", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ScreeningDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ScreeningDDDDependencies>(
      SCREENING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetScreeningSnapshot, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { snapshotId: entity.id };

    await getScreeningSnapshot(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetScreeningSnapshot.execute).toHaveBeenCalledWith(expect.objectContaining({ snapshotId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { snapshotId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetScreeningSnapshot, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getScreeningSnapshot(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
