/**
 * Attestation Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: attestations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { AttestationEntity } from "@kyvora/core/attestations/models/index.js";
import { attestationsSchemas } from "@kyvora/core/attestations";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "att";

export function createAttestation(overrides: Partial<AttestationEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    assuranceLevel: overrides.assuranceLevel ?? "IAL1",
    attestationId: overrides.attestationId ?? `att_${ulid().toLowerCase()}`,
    identityProviderId: overrides.identityProviderId ?? faker.string.uuid(),
    liability: overrides.liability ?? {
      bearer: "identityProvider",
      ruleRef: "",
      notes: ""
    },
    relyingPartyId: overrides.relyingPartyId ?? faker.string.uuid(),
    status: overrides.status ?? "pending",
    subjectId: overrides.subjectId ?? faker.string.uuid(),
    ...overrides,
  };

  return attestationsSchemas.Attestation.parse(raw);
}

export function attestationBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createAttestation(overrides));
}
